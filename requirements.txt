# Python libraries needed to run FPyVS_appylication.py, FPVS_pycage.py and cust_funcs.py
numpy
scipy
matplotlib
mne
h5py
plotly
trimesh
pillow
networkx
ipywidgets
ipython
jupyterlab
nbformat

# Tk is not a pip package: it comes with Python itself. The setup scripts check
# for it and, on Linux, install it. Windows: tick "tcl/tk and IDLE" in the
# Python installer. macOS: brew install python-tk. Linux: sudo apt install python3-tk
